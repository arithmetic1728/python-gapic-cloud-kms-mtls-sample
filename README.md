# python-gapic-cloud-kms-mtls-sample
A sample showing how to use device certificate to estalish mutual TLS connection with cloud KMS client.

`export FLASK_APP=web_sample.py`
`python -m flask run --port=3000`
